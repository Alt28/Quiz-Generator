services:
  - type: web
    name: clarko
    runtime: python
    buildCommand: bash build.sh
    startCommand: gunicorn CLARKO:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
